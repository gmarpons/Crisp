/* This file is a template that CMake uses to generate a config.h file
   in the build tree that contains some necessary definitions. See the
   CMakeLists.txt file in this directory */

/* Build tree directories. The following variables are set in
   ${CMAKE_SOURCE_DIR}/CMakeLists.txt */
#define BINARY_ROOT_DIR ${BINARY_ROOT_DIR}
#define BINARY_DATA_DIR ${BINARY_DATA_DIR}
#define BINARY_SWI_BOOT_FILES_DIR ${BINARY_SWI_BOOT_FILES_DIR}
#define BINARY_CRISP_RULES_DIR ${BINARY_CRISP_RULES_DIR}
#define BINARY_PROLOG_RULES_DIR ${BINARY_PROLOG_RULES_DIR}

/* Installation directories. The following variables are set in
   ${CMAKE_SOURCE_DIR}/CMakeLists.txt */
#define INSTALL_ROOT_DIR ${INSTALL_ROOT_DIR}
#define INSTALL_DATA_DIR ${INSTALL_DATA_DIR}
#define INSTALL_SWI_BOOT_FILES_DIR ${INSTALL_SWI_BOOT_FILES_DIR}
#define INSTALL_CRISP_RULES_DIR ${INSTALL_CRISP_RULES_DIR}
#define INSTALL_PROLOG_RULES_DIR ${INSTALL_PROLOG_RULES_DIR}

/* SWI Prolog directory. The following variable is set in
   ${CMAKE_SOURCE_DIR}/cmake/FindSwipl.cmake */
#define SWIPL_ROOT_DIR ${SWIPL_ROOT_DIR}
